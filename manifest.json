{
	"manifest_version": 2,

	"name": "MakeMeLater",
	"description": "This extension parses the current webpage for recipe data and writes the recipe to a specified file path.",
	"version": "1.0",

	"browser_action": {
		"default_icon": "icon.png",
		"default_title": "MakeMeLater"
	},
	"background": {
		"scripts": ["background.js"],
		"persistent": false
	},
	"permissions": [
		"notifications"
	]
}